# Imagens

As imagens dos eventos estao configuradas em `js/eventos.js` usando URLs externas.

Se quiser trocar por arquivos locais, coloque as imagens nesta pasta e altere a propriedade `imagem` de cada evento para caminhos como `img/nome-da-imagem.jpg`.
